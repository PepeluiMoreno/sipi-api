
 # Base de datos y ORM
# sipi-core>=0.1.0
sqlalchemy[asyncio]~=2.0.36
alembic~=1.14.0
asyncpg~=0.30.0
psycopg[binary]~=3.2.3
GeoAlchemy2~=0.15.2
# Note: 
# 1. PostgreSQL schemas 'sipi' and 'portals' must be created manually before running migrations
# 2. PostGIS extension must be installed in the database: CREATE EXTENSION IF NOT EXISTS postgis;

# GraphQL (versión compatible con Pydantic 2.11+)
strawberry-graphql[asgi]~=0.243.0
strawberry-sqlalchemy-mapper~=0.8.0
aiodataloader~=0.3.0

# Servidor web
uvicorn[standard]~=0.32.1
starlette~=0.41.3

# Utilidades
pandas>=2.2.0,<3.0.0
requests~=2.32.0
python-dotenv~=1.0.1
unidecode~=1.3.8

# Geospacial
Shapely~=2.1.2
geopy~=2.4.0
osmnx[all]~=2.0.7

# Pydantic (ajustado para compatibilidad)
pydantic~=2.9.2
pydantic-core~=2.23.2
httpx[http2]~=0.24.1

# FastAPI
fastapi~=0.128.0

# Environment variable management
python-dotenv~=1.0.1

# ===========================================
# DEPENDENCIAS ESPECÍFICAS PARA ETL/SCRAPING
# ===========================================

# Para extract_registradores.py (extracción web)
aiohttp~=3.10.0
beautifulsoup4~=4.12.0

# Para transformación de datos
openpyxl~=3.1.2  # Para leer Excel si es necesario
xlrd~=2.0.1      # Soporte para .xls

# Para carga masiva
psutil~=5.9.0    # Monitoreo de recursos

# Utilidades adicionales para ETL
tqdm~=4.66.0     # Barras de progreso
chardet~=5.2.0   # Detección de codificación

# Testing para scripts ETL
pytest~=8.3.2
pytest-asyncio~=0.24.0

# Logging mejorado
structlog~=24.3.0

# Manipulación de fechas
python-dateutil~=2.9.0

